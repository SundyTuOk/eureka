eureka:
  client:
    serviceUrl:
      defaultZone: http://slave1:8761/eureka/, http://slave2:8762/eureka/
  instance:
#    prefer-ip-address: true
spring:
  application:
    name: provider
server:
  port: 8002
